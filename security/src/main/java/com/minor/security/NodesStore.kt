package com.minor.security

import com.minor.model.NodesStore
import com.minor.model.KnownNode

/**
 * Re-exporting or keeping the file for package structure, 
 * but the main interface is now in :model to prevent circular dependencies.
 */
// If you prefer, this file can be deleted and usages updated to com.minor.model.NodesStore
